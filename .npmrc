# Vercel/pnpm monorepo compatibility:
# Creates physical node_modules in each package dir instead of symlinks to .pnpm store.
# Required for vercel deploy --prebuilt to find dependencies.
shamefully-hoist=true
strict-peer-dependencies=false
